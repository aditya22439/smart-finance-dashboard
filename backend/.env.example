MONGO_URI=mongodb+srv://username:password@cluster.example.mongodb.net/database?retryWrites=true&w=majority
PORT=5000
JWT_SECRET=replace-with-a-long-random-secret
# Optional: helpful on networks whose default DNS resolver cannot resolve MongoDB Atlas SRV records
DNS_SERVERS=8.8.8.8,1.1.1.1
